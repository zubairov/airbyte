version: 0.1.0
type: DeclarativeSource
check:
  type: CheckStream
  stream_names:
    - CS_RevOs_API
streams:
  - type: DeclarativeStream
    name: CS_RevOs_API
    primary_key:
      - id
    schema_loader:
      type: InlineSchemaLoader
      schema:
        $schema: http://json-schema.org/schema#
        properties:
          active_enabling:
            type: boolean
          arr:
            type: integer
          count_apps:
            type: integer
          count_connectors:
            type: integer
          count_modules:
            type: integer
          count_users_ext:
            type: integer
          count_users_int:
            type: integer
          freshdesk_id:
            type: string
          functions_idp:
            type: boolean
          hubspot_id:
            type: string
          id:
            type: integer
          instance_version:
            type: string
          license_package:
            type: string
          license_serial:
            type: string
          maintenance_from:
            type: string
          maintenance_till:
            type: string
          max_apps:
            type:
              - integer
              - string
          max_ext_users:
            type:
              - integer
              - string
          max_int_users:
            type:
              - integer
              - string
          name:
            type: string
          vtiger_no:
            type: string
        type: object
    retriever:
      type: SimpleRetriever
      name: CS_RevOs_API
      primary_key:
        - id
      requester:
        type: HttpRequester
        name: CS_RevOs_API
        url_base: http://run.simplifier.io/client/2.0/business-object/
        path: CS_RevOs_API/getData
        http_method: POST
        request_options_provider:
          type: InterpolatedRequestOptionsProvider
          request_parameters: {}
          request_headers: {}
          request_body_json:
            revos: rocks
        authenticator:
          type: BasicHttpAuthenticator
          username: '{{ config[''username''] }}'
          password: '{{ config[''password''] }}'
      record_selector:
        type: RecordSelector
        extractor:
          type: DpathExtractor
          field_pointer:
            - data
      paginator:
        type: NoPagination
spec:
  connection_specification:
    $schema: http://json-schema.org/draft-07/schema#
    type: object
    required:
      - username
      - password
    properties:
      username:
        type: string
        title: Username
      password:
        type: string
        title: Password
        airbyte_secret: true
    additionalProperties: true
  documentation_url: ''
  type: Spec
